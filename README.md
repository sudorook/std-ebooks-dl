# Standard eBooks Downloader

Crawl the [Standard eBooks website](standardebooks.org) and download all
available eBooks.

Requires:

- `bash`
- `curl`
- `grep`
- `sed`

Run:

```bash
./download.sh
```
